# DND五版不全书 · 现状快照（2026-08-05 由监督方从探索日志提取，供目标AI直接使用，勿重复探索）

> 本快照由监督方（opencode）从上一轮目标AI探索日志中沉淀。事实均来自真实文件读取结果。目标AI收到任务后直接基于本快照规划实施，禁止重新通读下列大文件全文（可针对性精读需要修改的函数）。

## 一、规则数据产物（compressed/，齐全）
- rule_settings.json：system=DND五版不全书，6797个HTML，topLevelSources含 玩家手册/玩家手册2024/城主指南/城主指南2024/怪物图鉴/怪物图鉴2025/多元宇宙的怪物/第三方/模组
- rule_index.json / rule_tables.md / rule_tree.json / rule_categories.json / rule_spell_lists.json / rule_equipment.json / ai_gm_index.md 均存在

## 二、现有插件（Ruler/DND五版不全书/plugins/）
| 插件 | manifest | index.js 规模 | 说明 |
|---|---|---|---|
| character-builder | type=character-sheet, "DND角色卡" | 1112行 | 角色卡创建/查看，最核心 |
| class-guide | type=panel, "职业升级速查" | ~10530字符 | 职业条目/等级表检索 |
| monster-guide | type=panel, "怪物速查" | ~15319字符 | 怪物条目检索 |
| spell-lookup | type=panel, "法术查询" | ~12623字符 | 法术条目检索 |

### character-builder/index.js 已确认事实（1112行）
- 属性生成：有 mode 切换（'pointbuy' 时 min=8；骰点模式 min=3），购点有 pbUsed>27 校验（"只能购买27点"）；标准数组/手动/4d6 模式存在与否需精读第650-700行附近确认
- 技能：SKILLS 数组存在，按 skillBonus 计算（已确认 982-984 行附近有 skills 数据与 bonus 计算）
- 保存：saveCharacter 回填后同步自动勾选与手动 chips（renderSpellArea 863行附近）；保存格式含 background、level、hitDice、HP:{current,max}（943-946行附近）
- 升级逻辑：有"应用升级：返回更新后的 data（保持现有保存格式，仅更新受等级影响的字段"（121行附近注释）——升级功能已有雏形

## 三、宿主文件（app/，白名单外只读）
- app/js/ui.js（2532行）关键点：
  - _loadSettings/_saveSettings：localStorage 'trpg_settings'（38-39行）
  - refreshCharacterList：1239行；deleteCharacter/selectCharacter/导入角色 1185行附近
  - damageCharacter(1297)/healCharacter(1300)：applyHPChange
  - setupPendingCharacterListener：1329行，监听 storage 事件 'trpg_pending_character'
  - 角色持久化：token 保存到 localStorage 'trpg_sheet_<id>'（1262、1323行），**但启动时从不恢复**（刷新页面角色丢失，仅最后查看的快照留在 localStorage 供 sheet 窗口用）——多角色持久化缺失是已知缺陷
- app/sheet.html：独立角色卡查看器（从 localStorage 'trpg_sheet_<id>' 读取并轮询更新），**不使用插件 renderDetail**
- app/js/map.js：地图引擎（网格/平移缩放/角色标记/范围显示），有 getAllTokens/updateToken 类接口（细节需grep）
- app/js/dice.js：存在（DiceSystem 掷骰模块，r122 正查看）
- app/js/app.js：宿主主逻辑（白名单外）
- app/index.html：含 battle panel（先攻/HP，r121 正查看 encounter/initiative 相关）

## 四、宿主渲染层（已由监督方新增，勿重复实现）
- app/js/ui.js simpleMarkdown 已实现 GM 标记渲染：<dice>→检定卡片、<battlecheck>→战斗卡片、<char_info>→角色档案卡片、<UpdateVariable>→折叠卡、<battle>→折叠地图、charGenerationThink 隐藏；字段解析器 parseFixedFields 支持中英字段名与多行值（2427行附近 renderDiceCard 等函数）
- app/css/style.css 已新增 gm-card/gm-result-*/gm-update-*/gm-char-* 系列样式（文件尾部）
- app/js/ai.js CORE_PROMPT 已升级为 GM 带团标准（8节），buildMessages 中固定作为 system 首部注入一次

## 五、明确差距（本轮任务重点，按优先级）
1. **多角色持久化缺失**：token 只存 trpg_sheet_<id> 快照、启动不恢复 → 需补 localStorage 角色列表持久化 + 启动恢复（或后端落盘）
2. **属性生成模式完整性**：确认 pointbuy/骰点/标准数组/手动 四模式是否齐全（精读650-700行）
3. **升级流程**：已有雏形，需按规则书验证派生值联动完整性
4. **战斗结算**：宿主 battle panel 存在，battlecheck 标记已渲染，验证可用性
5. **术语悬停预览/统一列表化**：速查类插件需统一 名称|来源|摘要 列表 + 点击新窗口 + 悬停提示

## 六、参考（勿读取大文件全文）
- GM标准：skill gm-standard（玩家频道实际注入提示词）
- 带团协议：skill gm-protocol（标记/数据/流程规范）
- 插件规范：skill plugin-authoring（界面定律/查询质量/角色卡标准）
