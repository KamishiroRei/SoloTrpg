# SoloTrpg — AI驱动的通用单人跑团平台

> 因为自己的团鸽了好像一个月于是忍不住捣鼓了这么个东西。  
> 理论上是接入AI之后让AI自己捣鼓并部署一套TRPG用的基类。  
> 暂时没考虑做成联机的，有兴趣的自己捣鼓。

## 1. 定位

SoloTrpg（TrpgRecode）是一个接入AI后自动运作的通用TRPG单人平台。系统只提供基础设施（地图、网格、骰子、显示容器），**所有游戏内容由AI读取规则书后动态生成**。

不预设任何特定规则系统。任何TRPG规则只需AI读取后即可自动适配——角色卡字段、法术系统、战斗规则全部由AI配置。

## 2. 文件书写原则

以下原则适用于本项目所有文档、注释和提交信息：

1. **功能性描述**：写这个文件/函数做什么、怎么用，不写"AI改了什么"、"相较于之前"之类的对比性描述
2. **当前事实**：以当前代码的真实状态为准，不写历史版本差异
3. **无自证**：不出现"AI已成功完成了X"、"经测试验证了Y"等自证句式
4. **无对比**：不出现"相较于v1版本"、"原来用A现在改成B"等解释性对比
5. **简洁直接**：每段第一句说清结论，细节在后

## 3. 规则书任务区

规则书不是被动上传的文件，而是**任务区**。AI在接入时自动扫描、接管对应目录：

```
Ruler/                        # 规则书任务区根目录
├── DND/                      # 每个规则系统一个子目录（任务区）
│   ├── source/               # 原始文本存档（PDF/CHM提取的原文）
│   ├── compressed/           # AI压缩的标准表格（日常查询用）
│   └── _index.json           # 自动生成的压缩索引
├── CoC/                      # 克苏鲁的呼唤
│   ├── source/
│   └── compressed/
└── SWADE/                    # 狂野世界
```

**自动接管流程**：
1. 用户将规则书PDF/CHM放入或上传到对应系统目录
2. 后端启动时自动扫描所有任务区，将文件归档到 `source/`
3. AI读取原始文本 → 压缩为标准化查表格式 → 存入 `compressed/`
4. 生成关键词索引 `_index.json`，支持模糊检索
5. 前端自动加载索引，游戏中AI按需检索而非全文读取

## 4. Token优化架构

核心策略：**压缩运行 + 原文存档 + 按需检索**

```
长文本（规则书原文）
  → AI压缩 → 标准化查表格式（每条1-2句核心机制）
  → 关键词索引（支持模糊检索）
  → 存入 compressed/ + _index.json
  
AI日常上下文
  → 只注入：游戏状态（一行式）+ 最近5条骰点 + 按需检索的规则压缩摘要
  → 单次上下文控制在 4000-6000 字符内
  → 不全文注入规则书
  
需要详细规则时
  → AI根据用户问题提取关键词
  → 模糊搜索压缩索引
  → 返回匹配条目的压缩摘要
  → 如果摘要不够，按偏移精确读取 source/ 原文段落
```

压缩格式（标准化查表）：

| 分类 | 条目 | 关键词 | 压缩摘要 |
|------|------|--------|----------|
| 职业 | 战士 | 生命骰,d10,重甲 | HP d10, 熟练: 重甲/军用武器, 2级获得动作如潮 |
| 法术 | 火球术 | 3环,塑能,8d6,150尺 | 150尺范围20尺半径, 8d6火焰, DEX豁免减半 |

## 5. 模块架构

```
├── index.html           # 单页应用
├── server.js            # Node.js后端（AI代理、规则扫描、压缩管道）
├── css/style.css        # 暗色主题UI
├── js/
│   ├── dice.js          # 通用骰子系统（XdY+Z, XdYkH, XdYklL）
│   ├── map.js           # 地图引擎（Canvas网格、标记、范围显示）
│   ├── compressor.js    # 文本压缩器（长文→结构化查表格式）
│   ├── rulesearch.js    # 规则搜索引擎（模糊检索、token优化注入）
│   ├── templates.js     # 通用模板渲染器（AI生成配置→动态角色卡）
│   ├── ai.js            # AI集成（多模型、上下文组装、骰点感知）
│   ├── ui.js            # UI管理（6个面板、聊天、弹窗）
│   └── app.js           # 主控制器
└── Ruler/               # 规则书任务区
```

### 5.1 骰子系统

支持 d4/d6/d8/d10/d12/d20/d100。表达式解析：`2d6+3`、`4d6k3`（取高）、`2d20kl1`（劣势）。真实掷骰记录到 `verifiedRolls`，AI严格区分真实掷骰和文字提及。

### 5.2 地图引擎

Canvas方形网格。平移/缩放。角色标记（圆形头像+HP条+名称）。范围显示：圆/锥/线/方。测量工具。网格吸附。

### 5.3 压缩器

`TextCompressor`：客户端提取文本结构（标题→条目），生成关键词。`fuzzySearch`：支持部分匹配、分类过滤。`searchToContext`：搜索结果压缩为紧凑上下文。

服务端 `parseMdToEntries`：从压缩MD表格反向解析结构化条目。`/api/rules/index`：返回完整压缩索引JSON。`/api/rules/source`：按偏移精确读取原文。

### 5.4 规则搜索引擎

`RuleSearch`：前端缓存压缩索引。`search()`：模糊搜索（关键词+标题+摘要匹配）。`buildAIContext()`：按用户查询+游戏状态按需检索规则，控制上下文长度。

自动发现：连接后端时自动扫描所有任务区，优先加载有压缩索引的系统。

### 5.5 模板渲染器

不接受硬编码字段。接受AI生成的配置JSON动态渲染角色卡。支持 grid/list/table/bar 四种渲染类型。

### 5.6 AI集成

多模型支持（OpenAI/GPTPRO/自定义）。上下文组装策略：游戏状态（一行式）+ 最近5骰点 + 规则压缩检索结果。AI系统提示词包含骰点识别规则。

## 6. 骰点识别

- 快捷按钮/表达式输入 → `DiceSystem.smartRoll()` → 记录到 `verifiedRolls`
- 用户聊天中写"1d20=15"但不经系统 → 不计入
- AI系统提示词："只有标记为已验证掷骰的结果才是真实掷骰"

## 7. 启动

```bash
cd TrpgRecode && npm install
node server.js
# 浏览器打开 http://localhost:3000
```

不启动后端也可使用基础功能（地图、骰子、手动管理）。AI功能需要后端服务。
